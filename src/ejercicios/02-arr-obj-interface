/*
    ===== Código de TypeScript =====
*/

let habilidades: (string | number | boolean)[] = ['Ronald', 92, true];
let habilidades2: string[] = ['Ronald', 'Carmin', 'Juana'];

interface Personaje {
    nombre: string;
    hp: number;
    habilidades: string[];
    puebloNatal?: string;
}

const personaje: Personaje = {
    nombre: 'Strider', 
    hp: 100,
    habilidades: ['Bash', 'Counter', 'Healing']
}

personaje.puebloNatal = 'Pueblo Paleta';

console.table(personaje);